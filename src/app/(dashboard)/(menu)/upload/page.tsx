import { Upload } from "lucide-react";
import React from "react";
import UploadForm from "./_components/UploadForm";

const page = () => {
  return (
    <div>
      <UploadForm />
    </div>
  );
};

export default page;
